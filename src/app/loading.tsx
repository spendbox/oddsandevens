/**
 * Deliberately blank. Every other route in this app paints instantly from
 * local data, so a spinner here would flash for one frame and read as a
 * stutter rather than as progress.
 */
export default function Loading() {
  return null
}
