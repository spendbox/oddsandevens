# Minimal Supabase CLI config so `supabase link` / `db push` / local `start`
# work from a fresh clone. Extend as needed (auth, storage, etc.).
project_id = "spendbox"

[db]
port = 54322
major_version = 17

[auth]
site_url = "http://localhost:3000"
